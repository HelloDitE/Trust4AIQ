version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}

  frontend:
    build: ./frontend/dashboard
    ports:
      - "5173:5173"
    depends_on:
      - backend